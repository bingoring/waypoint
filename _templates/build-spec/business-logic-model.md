---
artifact: business-logic-model
build-spec: <topic-slug>
status: DRAFT
updated: YYYY-MM-DD
---

# Business Logic Model — <제목>

> **무엇을 다루나:** **워크플로·알고리즘·상태 전이·시퀀스·통합 지점** — "어떻게 동작하는가"의 실행 모델. 기술 비종속
> (의사코드/도표 수준)으로, 구현자가 제어 흐름을 다시 설계하지 않게. 데이터는 `domain-entities.md`, 규칙은
> `business-rules.md`(여기선 규칙을 *언제/어떤 순서로* 적용하는지).

## 1. 주요 워크플로 (Workflows)
> 단계별 입력→처리→출력. 워크플로마다 반복.

### `<WorkflowName>`
1. **트리거:** <무엇이 시작시키나>
2. **단계:** 입력 → <처리1> → <처리2> → 출력
3. **결과/부작용:** <상태 변경·이벤트·저장>
4. **적용 규칙:** business-rules `R?` (어느 단계에서)

## 2. 알고리즘 (Algorithms)
> 핵심 계산/판정 로직을 의사코드로. (예: 등급 산정, 매칭, 정렬, 충돌 판정.)

```
<pseudo-code>
```

## 3. 상태 전이 (State Transitions)
> domain-entities의 상태 필드에 대한 상태 기계. `현재 × 이벤트 → 다음 + 액션`.

| 현재 상태 | 이벤트 | 다음 상태 | 액션/부작용 | 가드(business-rules) |
|---|---|---|---|---|

## 4. 시퀀스 / 상호작용 (Sequence)
> 컴포넌트/서비스/레이어 간 호출 순서. 필요 시 순서 목록 또는 시퀀스 도표.

## 5. 통합 지점 (Integration Points)
> 외부 API·서비스·큐 호출과 **실패 처리**(재시도/타임아웃/폴백).

| 대상 | 호출 시점 | 요청/응답 요지 | 실패 처리 |
|---|---|---|---|
