# duty — 결정 로그 (Decision Log / Audit)

> **append-only 감사 추적.** 중요한 결정 1건 = 항목 1개. *왜/대안/결정자*를 남겨, 최종 문서엔
> 증류되지 않는 "결정 provenance"를 보존한다. 스테이지 문서(*무엇을/결과*)·git 커밋과 상호보완.
> 원시 대화 트랜스크립트는 적재하지 않는다(노이즈·시크릿 위험). AI가 결정 시점마다 한 항목씩 추가한다.

형식: `날짜 · 제목` → 결정 / 근거 / 대안(탈락) / 결정자.

---

## 2026-09-26 · Waypoint 서브모듈 위치 = `waypoint/`
- **결정:** 메인 저장소(`bingoring/duty`) 루트의 `waypoint/`에 서브모듈로 마운트하고, 프로젝트 문서는
  `waypoint/projects/duty/`에 둔다. 핸드오프 원본은 `inputs/design-handoff_v1/`에 등록한다.
- **근거:** 사용자가 경로를 `waypoint`로 지정했다. 핸드오프는 버전별 디렉토리로 쌓는 forin 관례를 따른다.
- **대안(탈락):** README 예시의 `docs/dlc` 경로 — 사용자 지정과 다름.
- **결정자:** 사용자

## 2026-09-26 · 핸드오프 v1의 확정 사항을 SoT로 수용
- **결정:** 핸드오프 README「확정된 결정 사항」1~11번(1c 내 줄 강조형, 격자 위 신청(2a), 규칙 설정(2b),
  빨강=신청 반영/파랑=관리자 수정 외곽선, OFF·D·E·N 복수 선택, 관리자 전용 코멘트, 월 1회 주말 통 OFF 등)을
  재논의 없이 요구사항으로 수용한다. 1a/1b 시안은 구현 대상이 아니다.
- **근거:** 디자인 대화에서 사용자가 이미 명시적으로 확정한 항목이다.
- **대안(탈락):** 없음.
- **결정자:** 사용자(핸드오프 경유)

## 2026-09-26 · 1-1 미해결 질문 Q1~Q8 확정
- **결정:** Q1 MVP는 핵심 흐름(S1·S3·S4·S8·S9·S10·S11) 먼저, S2·S5·S6은 2차. Q2 협의 수정은 오프라인 협의 +
  관리자 반영(교환 요청 UI 없음). Q3 수간호사는 격자에 표시하되 듀티 인원·K-tass 계산에서 제외. Q4 주 5일은 하드
  제약이 아니라 월 기준 OFF 목표 + 누적 OFF 보정. Q6 데이터에 `ward`를 두고 운영은 응급실 단일 병동.
  Q7 배포·비용은 1-3에서 제시. Q8 비밀번호 분실은 관리자 재발급.
- **근거:** 핸드오프 화면 범위와 종이 근무표 운영(주별 근무일이 일정하지 않음)에 맞춘 AI 기본안을 사용자가 수용.
- **대안(탈락):** 11개 화면 동시 출시, 시스템 내 교환 요청 흐름, 주 5일 하드 제약.
- **결정자:** 사용자

## 2026-09-26 · 종이 근무표 컬럼 의미 (Q5)
- **결정:** 「보」 = 보수교육 이수. 「특휴/개원」 = 특별휴가 / 개원기념일 오프. "-" = 입사일이 최근이라 개원오프 대상 아님.
- **근거:** 사용자 답변. 특휴 산식은 요구사항 원문 §6-7.
- **대안(탈락):** 없음.
- **결정자:** 사용자

## 2026-09-26 · 요구사항 원문을 입력 SoT로 등록
- **결정:** `inputs/requirements_v3.md`(동부시립병원 근무배정방식 정리 v3)를 입력으로 등록한다. 핸드오프 README와
  원문이 충돌하면 원문과 종이 근무표를 우선 근거로 삼고, 충돌은 스테이지 문서에 명시해 사람에게 확인한다.
- **근거:** 핸드오프 README는 원문을 "요약"한 2차 자료이며, 실제로 부호 규약 요약에서 원문 일부와 어긋났다.
- **대안(탈락):** 핸드오프 README만 SoT로 사용.
- **결정자:** AI(권고)

## 2026-09-26 · 잔여치 저장 = append-only 원장
- **결정:** 누적 OFF·잔여 N·연차·특휴 등은 `BalanceEntry` 원장에 증감만 추가하고, 현재값은 합계로 파생한다.
- **근거:** 초기 입력·관리자 수정·월 정산·휴가 승인이 같은 값을 바꾸고, 연말 리셋 없이 무한 이월되므로 감사 추적과
  월 마감 취소(반대 항목 추가)가 필요하다.
- **대안(탈락):** 핸드오프 제안 `UserBalance` 스냅샷 — 변경 원인을 잃고 되돌리기가 어렵다.
- **결정자:** AI(권고), 1-2 승인 대기

## 2026-09-26 · 누적 OFF 부호 = 종이 규약, 실제 OFF 산입 범위 (D1·D2)
- **결정:** `누적 OFF = 이월 + (실제 OFF − 슬리핑오프 − 기준 OFF)`, 양수 = 기준보다 더 쉼(반납), 음수 = 덜 쉼(더 받음).
  실제 OFF에는 일반·보수교육·노조교육 OFF를 넣고, 슬리핑오프·특휴·개원오프·연차·휴가 일은 넣지 않는다.
- **근거:** 종이 근무표 2026-10의 10명 전원이 이 공식(기준 OFF 11, 대체공휴일 포함)과 일치했다.
  (`01-inception/evidence/paper-2026-10-settlement-check.py`)
- **대안(탈락):** 핸드오프 README `offCarry += (기준OFF − 실제OFF)` — 10명 중 3명만 일치. S2 설명 문구는 편차로 수정한다.
- **결정자:** 사용자

## 2026-09-26 · 특별휴가 산정 (D3)
- **결정:** 연 단위 `min(5, round_half_up(5 × 그해 재직 일수 / 365))`. 재직 일수는 달력 일수. 1/1(또는 입사 시)에 부여,
  미사용분은 12/31 소멸. 구간표 경계값을 테스트로 고정. 개원오프는 개원기념일 재직 시 1일.
- **근거:** 요구사항 원문 §6-7(사용자가 재확인 요청). 출근일 기준이면 최상단 구간(329~365일)에 도달할 수 없다.
- **대안(탈락):** AI 최초 기본안 "직전 1년 재직 일수로 1/1 부여" — 원문의 "년단위" 산식과 다름. 부여 시점·소멸은 AI 결정.
- **결정자:** 사용자(산식) / AI(부여 시점·소멸)

## 2026-09-26 · 종이 우측 컬럼 표시 = 잔여량, 노란 이름 표시 미재현 (D4·D5)
- **결정:** 특휴/개원·검진은 잔여량으로 표시한다. K-tass 노란 이름 표시는 격자에 재현하지 않고 간호사 관리 화면에서만 보여 준다.
- **근거:** S5 "내 휴가 잔여"와 기준 통일. 확정 시안 1c에 노란 표시가 없다.
- **대안(탈락):** 사용량 표시, 격자 이름 배경 강조.
- **결정자:** 사용자

## 2026-09-26 · 1-3 아키텍처 확정 (A1~A3)
- **결정:** 솔버 = OR-Tools CP-SAT(Python 무상태 서비스, TS `@duty/domain` 검사기가 규칙 SoT이며 솔버 결과를 항상 재검사).
  스택 = Next.js 16 + Tailwind v4 + PostgreSQL 16 + Drizzle + 자체 세션 인증, Docker Compose 한 벌. 배포 = 클라우드 VPS.
  인원 부족 달에는 기준 OFF를 덜 주고 누적 OFF를 음수로 이월하는 것을 허용하며, 대상자는 솔버가 공정성 목적으로 고른다.
- **근거:** 인원 여유 분석(신규 3인 배정 기간이 겹친 달은 최소 인원만으로 −6칸, 나이트 여유 1칸) → 하드 제약 보장·불가능
  판정·min-max 공정성이 필요. 간호사가 집에서 신청하려면 외부 접속이 필요.
- **대안(탈락):** TS 지역 탐색(불가능 판정 불가), HiGHS WASM MIP(원인 추출 어려움), 병원 내부 서버, 관리형 분산 배포.
- **결정자:** 사용자

## 2026-09-26 · 개인정보 = 공개 저장소에 두지 않음 (가명 처리)
- **결정:** waypoint·duty는 공개 저장소이므로 간호사 실명·사번·실제 근무 사진을 커밋하지 않는다. 문서·핸드오프·fixture의
  이름은 가명(한수정·박서연·정하늘 …)과 가상 사번(00101~00111)으로 치환하고, 종이 근무표 사진은 저장소에서 제외한다.
  실명 원본은 메인 저장소의 gitignore 폴더 `.local/`에만 둔다. 코드의 시드 데이터도 가명을 쓰고, 실제 명단은 로컬 파일
  (gitignore) 또는 배포 후 관리자 입력으로 넣는다. 이미 push된 이력은 이 프로젝트 커밋을 가명본 1개로 합쳐 다시 쓴다.
- **근거:** AI가 공개 여부를 확인하지 않고 실명 자료를 push했다(2026-09-26). 사용자가 "공개 유지 + 개인정보 제거"와
  "저장소엔 가명, 실명은 로컬 파일"을 선택했다.
- **대안(탈락):** 저장소 비공개 전환(waypoint의 다른 프로젝트까지 비공개가 됨), 그대로 두기.
- **결정자:** 사용자

## 2026-09-27 · 2-2 규칙 해석 (Q1~Q4)
- **결정:** Q1 수간호사는 평소 인원·K-tass에 넣지 않고, 교대 근무자만으로 최소 인원·K-tass를 못 채우는 날에만 최후의 수단으로
  수간호사 D를 센다(하드 위반 아님, 소프트 경고 `S-HEAD-FILL`, 솔버는 큰 벌점으로 회피). Q2 최대 연속 오프 기본값을 15일로 바꾸고
  관리자가 규칙 설정에서 수정한다. 휴가(LEAVE)는 연속 오프 일수에 세지 않되 연속을 끊지도 않는다. Q3 금지 패턴의 off 자리에는 쉬는 칸
  (OFF·연차·휴가)이 모두 해당한다. Q4 트레이닝 동일 근무는 신규·프리셉터 중 한 명이 연차·휴가인 날은 예외.
- **근거:** 사용자 답변 — "(수간호사 D는) 거의 안 넣는데, 정말 안 되면 넣어. 최후의 수단", "10일은 간호사끼리 말로 합의한 구두계약,
  15일 이상이 규정상 안 되는 것". 종이 10월 10/2 D에 교대 근무자가 1명뿐인 사례.
- **대안(탈락):** 1-1 Q3 그대로(10/2가 하드 위반), 연속 오프 10일 하드, N-연차-D 허용, 트레이닝 항상 동일.
- **결정자:** 사용자 (LEAVE 산입 방식은 AI 기본안)

## 2026-09-27 · 2-2 검사기 해석 (AI 결정)
- **결정:** 16시간 휴식은 서로 다른 근무 코드가 이어질 때만 검사한다. D→E→N 순환은 별도 검사하지 않고 솔버 소프트 항으로 넘긴다.
  반복 겹침 경고 기준 = max(4, ceil(2 × 쌍 평균)). 주말 통 OFF는 대상 월 안의 토·일만 센다. 연차·교육 신청 미반영은 하드,
  근무 옵션 신청 미충족은 소프트. 전월 꼬리 = `requiredTailDays(rules)`.
- **근거:** 종이 10월에 D-D(15.5h)·N-N(15h) 연속과 E-OFF-D가 정상 운영으로 존재한다. 반복 겹침은 종이 기준 평균 2.04회·최대 7회.
- **대안(탈락):** 모든 연속 근무에 16시간 적용(종이 전체가 위반), 고정 겹침 기준, 1-2 §9 "전월 말 3일".
- **결정자:** AI (2-2 READY 승인으로 확정)

## 2026-09-27 · 신규 3인 근무 기간 (Q5)
- **결정:** 3인 근무 = 신규(프리셉티) + 프리셉터 + 그 외 1명. 3인 배정 기간은 완전 신규 3주, 타 병원 경력 간호사 2주(등록 시 종류 선택,
  기간은 수정 가능). 기간이 끝난 뒤에도 신규가 처음 서는 N 3개는 3인으로 근무한다(떨어져 있어도 순서대로 세고, 기간 안의 N은 세지 않음,
  트레이닝 기간 안에서만). `Training.kind`와 규칙 수치 `experiencedTripleWeeks`=2·`tripleNightCount`=3을 추가한다.
- **근거:** 사용자 추가 설명 및 옵션 답변("3인 기간 뒤 처음 서는 N 3개").
- **대안(탈락):** 기간과 무관한 신규의 첫 N 3개, 기간 뒤 첫 나이트 연속 구간.
- **결정자:** 사용자

## 2026-09-27 · 달을 걸친 주말 · D/E/N 분포
- **결정:** 주말 통 OFF에서 달을 걸친 주말(예: 10/31 토·11/1 일)은 토요일이 속한 달로 센다. 그 달을 검사할 때 다음 달 1일 칸이 없으면
  토요일 OFF만으로 달성 예정으로 보고, 다음 달 검사에서 1일(일)이 근무이면 소프트 경고 `S-WEEKEND-CARRY`를 낸다. 또한 한 사람의 그달
  D·E·N 개수 차이(최대 − 최소)가 2를 넘으면 소프트 경고 `S-SHIFT-BALANCE`를 내고, 토글 `balanceShiftTypes`와 허용치
  `shiftBalanceTolerance`는 규칙 설정에서 바꾼다(D·E·N 합 9 미만인 달, 트레이닝 중 신규, 야간 전담은 제외). 솔버는 2-6에서 이 차이를 줄인다.
- **근거:** 사용자 답변 "토요일이 속한 달로 세자", "종이로 처리할 때 누구는 D만, 누구는 E만 근무하는 경우가 많아 불만이 있었다".
  종이 10월(가명)은 10명 중 1명(강도윤 D 4·E 7·N 6)만 차이 3이다.
- **대안(탈락):** 월 경계 주말은 세지 않음(READY 때 AI 결정), 병동 평균 대비 편차 기준(설명하기 어렵고 인원 변동에 흔들림).
- **결정자:** 사용자 (달성 예정 처리·허용치 2·제외 조건은 AI)

## 2026-09-27 · 이월 고려 확장
- **결정:** 달이 바뀌어도 이어지는 것을 검사기 입력으로 모두 받는다. ① 다음 달 근무표가 있으면 앞쪽 `requiredTailDays`일을 받아 앞 달을
  고칠 때도 금지 패턴·휴식·연속 N/OFF·N 후 OFF를 월 경계에서 검사한다(경계에 걸친 위반은 두 달 모두에서 보임). ② 주말 통 OFF 미배정은
  연속 개월 수로 이어 센다. ③ D·E·N 분포는 이번 달에 더해 최근 3개월 누적도 본다(허용치 + 개월 − 1). ④ 보수교육 연 1회·노조교육
  연 2회는 올해 앞선 달 이수 횟수에 이어 센다(하드). ⑤ 연차·특휴·개원오프·검진·병가는 월초 잔여를 넘으면 하드 위반. 특정 달에 묶인
  코드는 없으며 12월→1월, 28일 2월, 30일 달을 테스트로 고정했다.
- **근거:** 사용자 설명 "10월, 11월은 예시일 뿐, 이월되는 것에 대해 많은 것을 고려해야 한다".
- **대안(탈락):** 전월 한 달만 참조, 교육 연간 횟수·잔여 초과를 신청 단계(2-5)에서만 검증.
- **결정자:** 사용자(방향) / AI(항목 선정·누적 허용치·기간 3개월 기본값, 규칙 설정에서 수정 가능)

## 2026-09-27 · 2-3 근무표 조회 (Q1~Q4)
- **결정:** Q1 누적 OFF는 색 없이 부호로 표시하고 "다음 달 반납 n" / "다음 달 n일 더 받음" 설명을 붙인다(핸드오프의 양수 초록은
  D1 부호와 맞지 않음). Q2 동료 정보는 모두 공개한다(칸의 휴가 종류·검진 반차, 특휴/개원·검진·보 잔여 포함). Q3 "이번달 OFF"는
  정산 기준 OFF(일반·보수교육·노조교육). Q4 로그인 직후는 이번 달, 다음 달이 확정되면 헤더에 링크.
  AI 결정: 휴가 칩 "휴"·연차 칩 "연"(외곽선 없음), 칸 종류는 툴팁, 개발 시드에 종이 10월(가명)을 확정 근무표로.
- **근거:** 사용자 답변. 원문 §13 "모든 간호사는 서로의 오프 수 및 잔여 나이트 수를 확인할 수 있다", 종이 양식이 전 컬럼을 공개.
- **대안(탈락):** 핸드오프 색 그대로, 동료는 누적 OFF·잔여 N만, 격자 off 칸 전부 세기, 다음 달 확정 시 다음 달 우선.
- **결정자:** 사용자

## 2026-09-27 · 핸드오프 v2 수용 (turn-3 우선)
- **결정:** 핸드오프 v2를 최신 SoT로 등록한다(turn-3 > 2 > 1). ① 근무 조정(S9)은 모든 간호사가 접근하고 간호사 간 **근무 교환 요청**(3a: 사람·날짜 선택 → 재배정 → 당사자 전원 수락 시 즉시 반영, 셀 source `swap`)을
  둔다 — 1-1 Q2 "오프라인 협의 + 관리자 반영, 교환 요청 UI 없음"을 대체한다. ② 휴가 신청은 별도 화면 없이 S4 팝오버에 통합(3b)하므로
  MVP에 들어온다. 증빙 첨부는 없다(1-3 §6 `FileStore` 불필요). LeaveRequest에 `annual` 종류와 코멘트를 더한다. ③ 메뉴: 근무표 · 근무 신청 ·
  근무 조정 · 동료 현황 · 규칙 안내 / 관리자: 듀티 생성 · 간호사 관리 · 규칙 설정. 사이드바 하단에 「내 휴가 잔여」(연차·특별휴가·검진 반차·병가·
  누적 OFF·잔여 N)를 모든 화면에 둔다(누적 OFF는 2-3 Q1대로 색 없이). ④ 휴가 칩은 연차 포함 모든 휴가가 `휴`(#D8E6C3, 외곽선 없음), 종류는 툴팁.
  ⑤ 단계: 2-5 근무 신청·휴가, 2-7 근무 조정·월 마감(관리자), **2-8 근무 교환 요청(간호사) 신설**, 2-9 통합·E2E.
- **근거:** 사용자가 핸드오프 v2를 입력으로 추가하고 반영을 요청. 단계 분리는 사용자 답변("교환 요청을 별도 단계로").
- **개인정보:** v2 README·HTML의 실명·사번(16건)은 가명·가상 번호로 치환했고 종이 근무표 사진은 저장소에서 제외했다. 원본은 `.local/originals/inputs/design-handoff_v2/`.
- **대안(탈락):** 교환 요청을 2-7에 합침, 휴가 신청을 2차 범위로 유지.
- **결정자:** 사용자(핸드오프) / AI(가명 처리·잔여 카드 색)

## 2026-09-27 · 핸드오프 v3 수용 — 오늘 열 강조
- **결정:** S3 격자에서 오늘 날짜 열을 강조한다(날짜·요일 헤더 bg #0F6E63·흰 글자, 그 열의 모든 칸 bg #DDEFEB + 좌우 1px #0F6E63 → 세로 띠,
  주말 음영보다 우선, 보는 달이 이번 달일 때만). 날짜에 따라 달라지는 화면을 E2E에서 검증하려고 개발·테스트 환경에서만 `DUTY_FAKE_TODAY`로
  "오늘"을 고정할 수 있게 했다(운영에서는 무시). E2E는 2026-10-13으로 고정한다.
- **근거:** 사용자가 핸드오프 v3를 올리고 반영을 요청. 이전 E2E는 실제 날짜에 따라 기대값이 바뀌는 문제가 있었다.
- **개인정보:** v3 README·HTML의 실명·사번 16건은 가명 치환, 종이 근무표 사진은 제외했다. 원본은 `.local/originals/inputs/design-handoff_v3/`.
- **대안(탈락):** 브라우저 시계만 고정(서버 렌더링이라 효과 없음), 날짜 의존 E2E 생략.
- **결정자:** 사용자(핸드오프) / AI(날짜 고정 방식)

## 2026-09-27 · 2-4 관리자 설정 (Q1~Q4)
- **결정:** Q1 간호사 추가 패널과 수정 창에 연차 구분(필수)·노조 여부·근무 방식(기본 교대)·권한(기본 간호사)을 더한다. Q2 추가할 때 연차·잔여 N·
  이월 오프를 입력하고 특휴(산식)·검진 0.5·병가 60·개원오프(대상이면)를 자동 부여, 이후 값은 수정 창 "잔여치 조정"으로 바꾸며 원장에
  `admin_adjust` + 메모로 남긴다. Q3 공휴일은 공공데이터포털 「한국천문연구원_특일 정보」 `getRestDeInfo`로 가져오고(인증키 `HOLIDAY_API_KEY`는
  사용자가 발급해 `.env`에 둔다, 저장소에 넣지 않음), 병원 지정일·노사 협의일·개원기념일은 S11 아래 섹션에서 입력한다 — 1-3 §6 "API 연동은 MVP 제외"를
  대체한다. Q4 연초 잔여치 처리(연차 0으로 리셋, 특휴 새로 부여·미사용 소멸, 검진·병가 한도 갱신, 개원오프 재계산)는 새해 첫 요청에서 자동으로 하되,
  전년 12월이 확정·미마감이면 마감 뒤로 미룬다.
- **근거:** 사용자 답변. 12월 사용분이 원장에 들어가기 전에 리셋하면 마감 때 연차·특휴가 새해 잔여에서 한 번 더 차감된다.
- **대안(탈락):** 기본값으로 만든 뒤 수정 창에서만 입력, 별도 공휴일 메뉴, 관리자 버튼으로 연초 처리, 나중 단계로 미룸.
- **결정자:** 사용자 (미룸 조건·자동 부여 목록은 AI)
