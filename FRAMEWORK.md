# Waypoint Framework Rules

> 이 문서는 AI가 Waypoint 프레임워크 안에서 동작할 때 반드시 따라야 하는 계약서다.

## 세션 시작 프로토콜

AI가 새 세션을 시작할 때 반드시 아래 순서를 따른다:

1. `projects/{project-name}/STATUS.md`를 읽는다
2. `PENDING` 상태인 가장 앞 스테이지를 찾는다
3. 해당 스테이지 문서를 열고 `Inputs` 섹션에 명시된 파일을 모두 읽는다
4. `AI Proposal` 섹션을 작성하고 status를 `AI_PROPOSED`로 변경한다
5. 사람에게 리뷰를 요청하고 대기한다

## 절대 규칙

- `HUMAN_APPROVED` 없이는 다음 스테이지로 진행하지 않는다
- `AI Proposal` 섹션은 AI 전용이다. 사람이 직접 수정하면 안 된다
- 비가역 게이트(`⚠️` 표시)에서는 추가 체크리스트 항목을 반드시 확인한다
- 코드를 산출하는 Construction phase 이후, Operations 진입 전 **독립 코드 리뷰 게이트**(`🔍`)를 통과해야 한다 (아래 "리뷰 게이트")
- STATUS.md의 상태 표는 항상 실제 문서 상태와 동기화되어야 한다
- 중요한 결정이 내려지면 `projects/{name}/DECISIONS.md`에 항목을 추가한다 (아래 "결정 로그")

## 상태 생애주기

```
PENDING → IN_PROGRESS → AI_PROPOSED → HUMAN_APPROVED
```

| 상태 | 설명 |
|------|------|
| `PENDING` | 시작 전. 이전 스테이지가 APPROVED되지 않으면 진입 불가 |
| `IN_PROGRESS` | AI가 현재 작업 중 |
| `AI_PROPOSED` | AI 제안 완료. 사람의 검토 대기 |
| `HUMAN_APPROVED` | 사람이 승인. 다음 스테이지 언락 |

## 비가역 게이트 추가 체크리스트

`⚠️` 표시가 있는 스테이지는 아래 항목을 추가로 확인해야 승인 가능하다:

- [ ] 이 결정을 번복할 경우 영향받는 파일/레이어 목록을 작성했는가
- [ ] 대안을 검토하고 탈락 이유를 문서화했는가
- [ ] 외부 의존성(API, 라이브러리, 비용)을 확인했는가

## 리뷰 게이트 (Code Review Gate) 🔍

코드를 산출하는 Construction phase 이후, Operations(배포/운영) 진입 전에 **독립 코드 리뷰 게이트**를 둔다.
Waypoint 의 사람 승인 게이트는 *설계*는 검토하지만 *코드 자체*는 검토 공백이 있고, 코드를 쓴 AI 가
테스트 통과까지 자기보고하는 **자기확증 편향**이 있다. 이를 구조적으로 메운다.

### 독립성은 "태도"가 아니라 "구조"다 (가장 중요)

- 리뷰어는 **작성자(구현 주체)와 컨텍스트를 공유하지 않아야 한다.** 별도 서브에이전트 · 별도 세션 ·
  (가능하면) 다른 모델 · 또는 사람이어야 한다.
- **"적대적으로 리뷰하라"는 지시만으로는 부족하다.** 같은 컨텍스트의 AI 는 코드를 *있는 그대로*가 아니라
  *의도한 대로* 읽으며, "왜 이 줄이 있는지"를 알기에 의심 대신 합리화한다. 지시(태도)는 이미 독립적인
  리뷰어를 *증폭*시킬 뿐, 독립성을 *제조*하지 못한다.
- 따라서 **같은 컨텍스트의 작성자가 인라인으로 수행하는 자기리뷰는 이 게이트를 충족하지 못한다.**
  (가벼운 self-lint 정도의 보조 가치만 인정)

### 절차

1. 리뷰어에게 `_templates/reviewer-brief-template.md`(적대적 리뷰 지시 템플릿)를 채워 전달한다.
   - refute 기본값(불확실하면 결함으로 가정), 사냥 대상 체크리스트, severity 등급, 출력 형식을 포함.
2. 받은 findings 를 **맹목 수용하지 않는다.** 각 항목을 검증하여 채택/반박하고, 반박은 근거를 남긴다.
3. 진짜 결함은 **수정 + 회귀 테스트**를 추가한다.
4. 결과(채택/반박/수정)를 리뷰 스테이지 문서에 정리하고 `AI_PROPOSED` → 사람 승인.

### 적응형 깊이

- **단순/저위험**: 독립 리뷰어 1패스 + 단일 검증.
- **복잡/고위험**: 다관점(정확성·보안·동시성·운영) 적대 리뷰, 핵심 결함은 다수결 검증.

> 깊이는 조절하되 **독립성(컨텍스트 분리)은 타협하지 않는다.** 얕은 리뷰라도 독립이어야 의미가 있다.

템플릿: 스테이지 문서 `_templates/review-stage-template.md`, 리뷰어 지시 `_templates/reviewer-brief-template.md`.

## STATUS.md 업데이트 규칙

스테이지 상태가 변경될 때마다 STATUS.md의 테이블을 함께 업데이트한다.

## 문서 깊이 — 린 기본 + 적응형 깊이

스테이지 문서는 **기본은 경량(간결)**하게 쓴다. "문서가 일에 맞춘다" — 복잡도에 깊이를 맞춘다.

- **단순 스테이지**: 목적·입력·체크리스트·간단한 AI 제안으로 짧게.
- **복잡·고위험 스테이지**: AI 제안에 다음을 추가한다.
  - **컴포넌트/모듈 분해**
  - **NFR · 성능 목표**
  - **테스트·검증 계획** (순수 로직은 자동화 테스트로 검증, 시각/수동은 명시)

> 균일하게 얇으면 복잡 단계의 설계·근거가 사라진다. 반대로 모든 단계를 무겁게 쓰면 의식(ceremony)이 되어
> 속도를 깎는다. 깊이는 단계별로 판단한다.

## 구현 스펙 (Build Spec)

스테이지 문서의 `AI Proposal`은 **무엇을·왜**(설계 결정)를 다룬다. 하지만 실제 **로직·화면·알고리즘·콘텐츠**를 산출하는
스테이지는, 결정만으로는 구현 시 매번 SoT(핸드오프/PRD/디자인)를 재해석하게 되고 **누락·불일치**가 조용히 생긴다.
이를 막기 위해 **코딩 전에 구현 스펙(Build Spec)** 을 쓴다. (AI-DLC functional-design를 우리 방식으로 압축한 것 —
멀티 문서·유닛 병렬분해·persona 없이, **한 문서·고정 정규 섹션**으로.)

### 트리거 — 언제 Build Spec이 필요한가
- **필수**: 스테이지가 실제 로직/화면/알고리즘, 또는 콘텐츠 시리즈(예: 부서 인테리어)를 산출한다.
- **불필요(린 유지)**: 순수 설계·결정·조사 스테이지 → 기존처럼 `AI Proposal`만.

### 원칙
1. **SoT 1:1 유도.** Build Spec은 SoT를 "재해석"이 아니라 라인 단위로 유도해, **다시 유도하지 않고 바로 구현 가능한**
   수준으로 구체화한다(타입/좌표/props/데이터 shape/엣지케이스까지).
2. **고정 정규 섹션 = 완결성 체크리스트.** 자유 서술은 에러 처리·데이터 계약·엣지케이스를 조용히 누락시킨다. 정규
   섹션(개요·분해·도메인/데이터·동작/규칙·UI/상호작용·엣지케이스·명세표·미해결질문·체크리스트·검증·편차)을 뼈대로
   쓰고, 무관한 섹션은 지우지 말고 **`N/A + 사유`**(의도적 생략 증명). 도메인 특화는 이 뼈대의 인스턴스다.
3. **적응형 깊이 티어.** `minimal | standard | comprehensive` — 티어별 필수 섹션은 템플릿 표에 정의.
4. **Plan-first + 옵션형 질문 루프.** 애매함은 §미해결 질문에 모으고 **구현 착수 전 `AskUserQuestion`(옵션형)으로 해소**,
   확정 결정은 `DECISIONS.md`로 이관(원시 트랜스크립트는 적재하지 않음). 미해소면 `status: QUESTIONS_OPEN`, 코딩 금지.
5. **체크리스트로 실행 추적.** §구현 체크리스트를 **구현하며 즉시 체크**. 전 항목 + §검증 통과 = `IMPLEMENTED`.
6. **편차는 승인 후 기록.** SoT 대비 의도적 편차만 §편차 로그에. 미승인 불일치는 편차가 아니라 버그다.

### 위치 (하이브리드)
- **작으면 인라인**: 스테이지 문서 `AI Proposal` 안에 정규 섹션으로.
- **크면 별도**: `projects/{name}/{phase}/<topic>/`(예: `02-construction/departments/`) — README(공통 규약·템플릿·인덱스)
  + 항목별 `<item>.md`. 스테이지 문서에서 링크.
- 상태 흐름: `DRAFT → QUESTIONS_OPEN → READY → IMPLEMENTED`. 스테이지 승인 게이트는 **READY**(구현 착수 승인) 후
  구현, 그리고 코드 산출물은 기존 **리뷰 게이트 🔍**를 그대로 통과한다.

> 템플릿: `_templates/build-spec-template.md`. 실사례: `projects/forin/02-construction/departments/`.

## 결정 로그 (Decision Log)

각 프로젝트는 `projects/{name}/DECISIONS.md`(**append-only, ADR 스타일**)를 둔다.

- **중요한 결정마다 AI가 한 항목**을 추가: `날짜 · 결정 · 근거 · 탈락한 대안 · 결정자`.
- **원시 대화 트랜스크립트는 적재하지 않는다**(시크릿·노이즈). 큐레이션된 "왜/대안"만.
- STATUS.md에서 링크한다. 스테이지 문서(*무엇을/결과*)·git 커밋과 상호보완하는 **감사 추적**이다.
- 템플릿: `_templates/decisions-template.md`.
